[project]
name = "min-dados"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "agno",
    "ollama",
    "openai",
    "python-dotenv>=1.1.1",
]
